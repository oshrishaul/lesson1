print(1)
name = 'oshri'
age = 10
print(name + str(age))
print(name + str(age + age))

print(name, age)

print(age + 1)
from statements
# this is how to wright a comment, which will ignored by the commander
# ctrl + / will convert all the selected rows to comments
